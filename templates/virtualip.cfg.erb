<% @data.each_pair do |key, value_hash| %>
define host {
        host_name   <%= key %>
        address     <% value_hash["virtualip"].uniq.each do |val| %><%= val %><% end %>
        use         generic-virtual-host
        parents     <%= value_hash["parent"].uniq.join(",") %>
}
<% end %>